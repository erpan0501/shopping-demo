# Java 商城项目阅读指南

## 本地演示入口

项目的运行方式、端口和演示范围请先阅读根目录的 [README.md](README.md)。本指南仅用于阅读仓库中保留的 Java 扩展模块源码。

## 整体结构

```mermaid
flowchart LR
    Manager[商家后台 API] -->|Dubbo| Admin[权限服务]
    Manager -->|Dubbo| Goods[商品服务]
    Manager -->|Dubbo| Category[广告服务]
    UserAPI[用户端 APIs] -->|Dubbo| User[用户服务]
    UserAPI -->|Dubbo| Search[搜索服务]
    UserAPI -->|Dubbo| Cart[购物车服务]
    UserAPI -->|Dubbo| Order[订单服务]
    UserAPI -->|Dubbo| Seckill[秒杀服务]
    UserAPI -->|Dubbo| Custcare[AI 客服]
    Goods -. RocketMQ .-> Search
    Goods -. RocketMQ .-> Cart
    Order --> Pay[支付服务]
    Custcare --> Search
    Custcare --> Order
    Webhook[SkyWalking WebHook] --> Mail[邮件服务]
```

## 建议阅读顺序

1. 先看 `shopping_common` 中的实体和服务接口，理解系统词汇。
2. 再选一条业务路线：商品管理、购买、秒杀、AI 客服或监控告警。
3. 从 `*_api/controller` 进入，沿 `@DubboReference` 找到对应 `*_service` 实现。
4. 最后深入 Mapper、Redis、RocketMQ 或 Elasticsearch。

## 当前源码中特别值得注意

- `shopping_search_service` 的 `SyncGoodsListener` 和 `DelGoodsListener` 上，RocketMQ 监听注解当前被注释。
- 服务运行参数主要来自 Nacos，仓库中的 `bootstrap.yml` 只保存配置中心地址和前缀，因此端口、数据源等运行配置不完整。
- 原压缩包中的 `target` 和 SkyWalking Agent 没有解入源码目录；原始 `shopping.rar` 仍完整保留。
